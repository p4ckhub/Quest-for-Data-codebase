#include "spellbook.h"
#include <iostream>

// Compile-time check: verify spellbook.h is valid C++
// This file includes the generated spellbook.h to verify it compiles without errors.
// The spellbook.h contains namespace-wrapped spell implementations.

int main() {
    // If this compiles, the spellbook is valid
    std::cout << "Spellbook validation passed." << std::endl;
    return 0;
}
