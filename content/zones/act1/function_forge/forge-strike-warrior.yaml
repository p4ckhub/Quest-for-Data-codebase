id: forge-strike-warrior
kind: program
zone: function_forge
act: 1
title: "Forge Your First Spell"
concept: functions_basics
concepts_required: [main_function, variables_and_types, functions_basics]

teaching: |
  A spell is a *function*: a named working, forged once, cast whenever its
  name is spoken. Every forging takes the same shape:

  ```cpp
  int ember() {
      return 3;
  }
  ```

  Read the forging marks: `int` is the promise — this spell, when cast, yields
  a whole number. `ember` is its name. The `()` is the mouth of the spell (empty,
  for now — some spells take offerings). Between `{` and `}` lies the working
  itself, and `return 3;` is the yield — the spell hands back 3 to whoever cast
  it. A spell that promises an `int` and returns nothing fizzles at the forge.

narrative: |
  The Forge awaits your first true spell. A {{class_name}}'s strike
  must strike with force between 5 and 10 — steady enough to trust,
  wild enough to fear. The testing lines in main are carved by the
  realm; they will weigh your forging.

objective: |
  Complete the function strike so it returns a value between 5 and 10
  (inclusive). The carved lines in main will cast it and read its force
  aloud.

starter_code: |
    // TODO: Bring in the tool that lets you speak to the realm.
    // TODO: Forge int strike() to return any value between 5 and 10 inclusive.
    // TODO: Open a main function.
    // TODO: Leave the carved lines untouched -- they cast strike and read its force.
    // TODO: Return 0 to seal the rite.
solution: |
  #include <iostream>

  int strike() {
      return 7;
  }

  int main() {
      // [carved by the realm] The Forge weighs your strike:
      int force = strike();
      std::cout << "force " << force << "\n";
      return 0;
  }

validation:
  checks:
    - { type: exit_status, expect: clean }
    - { type: stdout_matches, id: force_in_range, regex: "force ([5-9]|10)(\\n|$)" }

hints:
  - trigger: "error:missing_return"
    message: "The spell fizzles — a function promising an int must 'return' one."
  - trigger: "check_failed:force_in_range"
    message: "The Forge-master frowns: the strike must land between 5 and 10 — a force above 10 would shatter your {{weapon}}."
  - trigger: "attempt:6"
    message: "Simplest honest spell: return 7;  — a fixed force is a fine first forging."

grants_spell:
  name: "{{starter_spell}}"
  signature: "{{starter_spell}}"

rewards:
  xp: 100
  unlocks_zone_progress: true
